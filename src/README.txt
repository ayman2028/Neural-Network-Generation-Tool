After calling the make command on proj3 files given by the professor, 
the only thing we did was to update the c++ code.
Testmode files stayed the same.

Every generated module (either from part 1, 2 or 3) will include the following helper files: 
(controller.sv and memory.sv) and (datapath_gen_p3_relu.sv if relu) or (datapath_gen_p3.sv if non-relu)

The helper files have been included in the src and hdl folders